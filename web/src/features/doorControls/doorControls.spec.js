import { triggerDoor, makeInitialState } from "./doorControlsSlice";

describe('Door Controls reducer', () => {
    
    it('should handle initial state',async () =>  {
      const initialState = makeInitialState()
    });

});